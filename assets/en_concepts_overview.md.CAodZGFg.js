import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.DnrXFDHb.js";const g=JSON.parse('{"title":"Overview","description":"","frontmatter":{},"headers":[],"relativePath":"en/concepts/overview.md","filePath":"en/concepts/overview.md"}'),n={name:"en/concepts/overview.md"};function r(o,e,l,h,p,c){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h1><p><strong>можно.</strong> is an open-core feature flag management platform. This page gives a high-level overview of all core concepts and how they fit together.</p><h2 id="architecture-at-a-glance" tabindex="-1">Architecture at a Glance <a class="header-anchor" href="#architecture-at-a-glance" aria-label="Permalink to &quot;Architecture at a Glance&quot;">​</a></h2><pre class="mermaid">graph TB
    Dashboard[Web Dashboard] --&gt; API[REST API]
    SDKs[Java &amp; JS/TS SDKs] --&gt; API
    API --&gt; Core[mozhno-core]
    Core --&gt; SPI[mozhno-spi]
    Core --&gt; DB[(PostgreSQL)]
    SPI --&gt; Plugins[Enterprise Plugins]
</pre><p>The server is a Spring Boot 4.0 application built on JDK 25 with a React 19 SPA frontend. It uses <code>JdbcTemplate</code> for database access (no JPA) and Flyway for schema migrations.</p><h2 id="core-concepts" tabindex="-1">Core Concepts <a class="header-anchor" href="#core-concepts" aria-label="Permalink to &quot;Core Concepts&quot;">​</a></h2><h3 id="flags" tabindex="-1">Flags <a class="header-anchor" href="#flags" aria-label="Permalink to &quot;Flags&quot;">​</a></h3><p>A <strong>flag</strong> (feature toggle) controls whether a feature is enabled or disabled for a given request. Flags come in two types:</p><ul><li><strong>Boolean</strong> — simple on/off toggle</li><li><strong>Multivariate</strong> — returns one of several predefined values (e.g., a string, number, or JSON)</li></ul><p>Flags are evaluated in real time by the SDK using locally cached rules — no network calls on the hot path.</p><p>See <a href="/en/concepts/flags.html">Flags</a>.</p><h3 id="segments" tabindex="-1">Segments <a class="header-anchor" href="#segments" aria-label="Permalink to &quot;Segments&quot;">​</a></h3><p>A <strong>segment</strong> is a reusable group of users defined by matching rules. Instead of repeating the same targeting conditions across multiple flags, you define a segment once and reference it from any flag.</p><p>Examples: &quot;beta testers&quot; (users with <code>beta: true</code>), &quot;EU users&quot; (users where <code>country</code> is in a list of EU country codes), &quot;internal employees&quot; (users with email ending in <code>@company.com</code>).</p><p>See <a href="/en/concepts/segments.html">Segments</a>.</p><h3 id="strategies" tabindex="-1">Strategies <a class="header-anchor" href="#strategies" aria-label="Permalink to &quot;Strategies&quot;">​</a></h3><p>A <strong>strategy</strong> determines <em>how</em> a flag is rolled out. Strategies are pluggable — the platform ships with three built-in strategies and supports custom ones via SPI:</p><ul><li><strong>Default</strong> — the flag is either on or off for everyone</li><li><strong>Gradual</strong> — progressively increase the percentage of users who see the flag</li><li><strong>Scheduled</strong> — enable the flag between specific dates/times</li><li><strong>Custom</strong> — implement any rollout logic via the SPI extension point</li></ul><p>Strategies are chained: if the first strategy doesn&#39;t match, the next one is tried. If none match, the flag defaults to off.</p><p>See <a href="/en/concepts/strategies.html">Strategies</a>.</p><h3 id="environments" tabindex="-1">Environments <a class="header-anchor" href="#environments" aria-label="Permalink to &quot;Environments&quot;">​</a></h3><p>An <strong>environment</strong> is an isolated namespace for flag configuration — typically <code>dev</code>, <code>staging</code>, and <code>production</code>. Each environment has:</p><ul><li>Its own set of flag configurations (a flag can be on in dev but off in prod)</li><li>Its own API keys with granular permissions</li><li>Independent targeting rules</li></ul><p>See <a href="/en/concepts/environments.html">Environments</a>.</p><h3 id="api-keys" tabindex="-1">API Keys <a class="header-anchor" href="#api-keys" aria-label="Permalink to &quot;API Keys&quot;">​</a></h3><p><strong>API keys</strong> are used by SDKs and external services to authenticate with the server. Keys are scoped to a single environment and can have read, write, or admin permissions. You create and manage API keys from the web dashboard.</p><p>A typical setup:</p><ul><li>Dev environment: read+write key for developer machines</li><li>Staging: read-only key for CI/CD pipelines</li><li>Production: read-only key for the live application, admin key for the operations team</li></ul><h3 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h3><p><strong>Context</strong> is the set of attributes passed to the SDK during flag evaluation. It describes the current user or request and is used by targeting rules and segments to decide whether a flag should be enabled.</p><div class="language-java vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> EvaluationContext</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;userId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;user-123&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;country&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;DE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;plan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;enterprise&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;beta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Context can include any arbitrary key-value pairs. Common attributes: user ID, email, country, subscription tier, device type, user agent.</p><h3 id="audit-log" tabindex="-1">Audit Log <a class="header-anchor" href="#audit-log" aria-label="Permalink to &quot;Audit Log&quot;">​</a></h3><p>Every change to flags, segments, strategies, environments, and API keys is recorded in an immutable <strong>audit log</strong>. Each entry includes:</p><ul><li>The user who made the change</li><li>What was changed (field-level diff)</li><li>Timestamp of the change</li><li>The environment where the change was applied</li></ul><p>The audit log is accessible from the web dashboard and via the REST API, giving teams full visibility into configuration history.</p><h2 id="how-flag-evaluation-works" tabindex="-1">How Flag Evaluation Works <a class="header-anchor" href="#how-flag-evaluation-works" aria-label="Permalink to &quot;How Flag Evaluation Works&quot;">​</a></h2><pre class="mermaid">sequenceDiagram
    participant App as Your Application
    participant SDK as SDK (Local Cache)
    participant Server as можно. Server

    App-&gt;&gt;SDK: isEnabled(&quot;new-checkout&quot;, ctx)
    SDK-&gt;&gt;SDK: Check cached rules
    alt Cache valid
        SDK--&gt;&gt;App: true/false
    else Cache stale
        SDK-&gt;&gt;Server: Fetch flag rules
        Server--&gt;&gt;SDK: Rules + segments
        SDK-&gt;&gt;SDK: Evaluate locally
        SDK--&gt;&gt;App: true/false
    end
</pre><ol><li>Your application calls the SDK with a flag key and evaluation context</li><li>The SDK checks its local cache of flag rules</li><li>If the cache is fresh, the SDK evaluates the flag locally in microseconds</li><li>If the cache is stale, the SDK fetches updated rules from the server, then evaluates locally</li><li>The SDK caches the new rules for subsequent calls</li></ol><p>This local-evaluation architecture eliminates network latency on every flag check.</p><h2 id="module-architecture" tabindex="-1">Module Architecture <a class="header-anchor" href="#module-architecture" aria-label="Permalink to &quot;Module Architecture&quot;">​</a></h2><p><strong>можно.</strong> is organized as a multi-module Maven project:</p><table tabindex="0"><thead><tr><th>Module</th><th>Purpose</th></tr></thead><tbody><tr><td><code>mozhno-spi</code></td><td>Service Provider Interface — extension points for enterprise plugins</td></tr><tr><td><code>mozhno-core</code></td><td>Core business logic: flag evaluation, segment matching, strategy engine</td></tr><tr><td><code>mozhno-web-api</code></td><td>REST controllers, DTOs, request/response mapping</td></tr><tr><td><code>mozhno-app</code></td><td>Spring Boot application entry point, auto-configuration, Flyway migrations</td></tr></tbody></table><p>The SPI layer is the foundation of the open-core model. Community features live in <code>mozhno-core</code>; enterprise features (SSO, billing, webhooks) are implemented as SPI plugins loaded at startup.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="/en/concepts/flags.html">Flags</a> — flag types, rules, and lifecycle</li><li><a href="/en/concepts/segments.html">Segments</a> — reusable user groups</li><li><a href="/en/concepts/strategies.html">Strategies</a> — rollout strategies and chaining</li><li><a href="/en/concepts/environments.html">Environments</a> — environment isolation and API keys</li></ul>`,46)])])}const u=a(n,[["render",r]]);export{g as __pageData,u as default};
